# PromptFlow — Production environment (Proxmox VM)
# Copy to .env on the VM: cp .env.production .env

POSTGRES_USER=promptmgr
POSTGRES_PASSWORD=PromptFlow2025!Prod
POSTGRES_DB=promptmanager

# Standard ports (no conflicts on dedicated VM)
DB_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=3000
